extends ../layout.pug

block content 
    br
    if user.avatarUrl
        img(src="/" + user.avatarUrl, width="100", height="100") 

    h3=user.name
    h3=user.email

    if loggedIn
        a(href=`/user/profile/${user.username}/edit`) Edit your profile