extends ../layout.pug

block content 
    br
    if loggedInUser.avatarUrl
        img(src="/" + loggedInUser.avatarUrl, width="100", height="100") 

    h3=loggedInUser.name
    h3=loggedInUser.email

    if loggedIn
        a(href=`/user/profile/${loggedInUser.username}/edit`) Edit your profile