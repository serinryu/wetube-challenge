mixin movie(movie)
    a(href=`../movies/${movie.id}`).video-mixin
        img(src=movie.thumbUrl, crossorigin).video-mixin__thumb
        div.video-mixin__data
            span.video-mixin__title=movie.title
            div.video-mixin__meta=`조회수 ${movie.meta.views}`